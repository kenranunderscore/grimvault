const config = {
    useTabs: false,
    tabWidth: 4,
    singleQuote: false,
    trailingComma: "all",
    printWidth: 100,
    plugins: ["prettier-plugin-organize-imports", "prettier-plugin-tailwindcss"],
};

export default config;
